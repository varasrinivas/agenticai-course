# Tier 2: GCP Cloud Build CI/CD Pipeline
# Bronze Layer Data Validation Pipeline
#
# Usage: gcloud builds submit --config=cloudbuild.yaml .
# Docs: See solution/deploy/gcp/cloudbuild.yaml for reference

# TODO 1: Define build steps
#   steps:
#     # Step 1: Build the Docker image
#     - name: 'gcr.io/cloud-builders/docker'
#       args: ['build', '-t', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/${_SERVICE_NAME}:${SHORT_SHA}', '.']
#
#     # Step 2: Push the image to Artifact Registry
#     - name: 'gcr.io/cloud-builders/docker'
#       args: ['push', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/${_SERVICE_NAME}:${SHORT_SHA}']
#
#     # Step 3: Deploy to Cloud Run
#     - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
#       entrypoint: gcloud
#       args:
#         - 'run'
#         - 'deploy'
#         - '${_SERVICE_NAME}'
#         - '--image'
#         - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/${_SERVICE_NAME}:${SHORT_SHA}'
#         - '--region'
#         - '${_REGION}'
#         - '--memory'
#         - '1Gi'
#         - '--timeout'
#         - '900'

# TODO 2: Define substitution variables
#   substitutions:
#     _REGION: us-central1
#     _REPO_NAME: bronze-validator-repo
#     _SERVICE_NAME: bronze-validator

# TODO 3: Configure build options
#   options:
#     logging: CLOUD_LOGGING_ONLY

# TODO 4: Push built images to Artifact Registry
#   images:
#     - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/${_SERVICE_NAME}:${SHORT_SHA}'
