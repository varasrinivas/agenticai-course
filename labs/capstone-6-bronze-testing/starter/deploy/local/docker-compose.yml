# Tier 1: Local Docker Deployment
# Bronze Layer Data Validation Pipeline
#
# Usage: docker compose up --build
# Docs:  See solution/deploy/local/docker-compose.yml for reference

version: "3.9"

services:
  # TODO 1: Define bronze-validator service with build context pointing to ../../
  #   - image name: bronze-validator:latest
  #   - build context should be the project root (../../)
  #   - Dockerfile path: ./Dockerfile
  #
  # TODO 2: Mount mock_data volume and output directory
  #   - Mount ./mock_data as /app/mock_data (read-only)
  #   - Mount ./output as /app/output (read-write)
  #
  # TODO 3: Set environment variables (ANTHROPIC_API_KEY, DUCKDB_PATH)
  #   - ANTHROPIC_API_KEY from host env or .env file
  #   - DUCKDB_PATH=/app/output/bronze.duckdb
  #   - LOG_LEVEL=INFO
  #
  # TODO 4: Add report-viewer service on port 8080
  #   - Use nginx:alpine image
  #   - Mount ./output as /usr/share/nginx/html (read-only)
  #   - Expose port 8080:80
  #   - Depends on bronze-validator completing successfully
