{
  "$comment": [
    "Hook structure is three levels: event -> {matcher, hooks:[...]} ->",
    "{type, command, timeout}. The command NEVER sits on the matcher --",
    "that flatter shape is valid JSON, saves without complaint, and never",
    "fires.",
    "",
    "PreToolUse  Write : blocks writes to data/production/",
    "PostToolUse Bash  : logs every shell command to audit.log",
    "",
    "MCP servers are NOT configured here. settings.json has no mcpServers",
    "key -- project-scoped servers live in .mcp.json at the project root."
  ],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "python .claude/hooks/block_production_writes.py",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python .claude/hooks/audit_log.py",
            "timeout": 10
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Read",
      "Glob",
      "Grep",
      "Write(src/**)",
      "Write(tests/**)",
      "Edit(src/**)",
      "Edit(tests/**)",
      "Bash(python *)",
      "Bash(npm *)",
      "Bash(git *)"
    ],
    "deny": [
      "Write(data/production/**)",
      "Bash(rm -rf *)",
      "Bash(curl *)"
    ]
  }
}
