{
  "$comment": [
    "The STRUCTURE below is correct and already filled in -- you fill in the",
    "VALUES marked TODO.",
    "",
    "Do not flatten the hooks. It is three levels:",
    "  event -> {matcher, hooks:[...]} -> {type, command, timeout}",
    "Putting `command` directly on the matcher is valid JSON, saves without",
    "complaint, and never fires. That is the failure mode this lab exists to",
    "make you recognise: nothing errors, the guardrail simply is not there.",
    "",
    "Hooks receive their payload as JSON on STDIN. There is no $TOOL_INPUT",
    "shell variable -- do not pass arguments on the command line.",
    "",
    "MCP servers do NOT go in settings.json. Use .mcp.json at the project root."
  ],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "TODO: which tool writes files?",
        "hooks": [
          {
            "type": "command",
            "command": "TODO: python .claude/hooks/<script>.py — block writes to data/production/",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "TODO: which tool runs shell commands?",
        "hooks": [
          {
            "type": "command",
            "command": "TODO: python .claude/hooks/<script>.py — append to audit.log",
            "timeout": 10
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "TODO: add the read tools (Read, Glob, Grep)",
      "TODO: add scoped write permissions (Write and Edit for src/ and tests/)",
      "TODO: add allowed shell commands (python, npm, git)"
    ],
    "deny": [
      "TODO: block writes to production data",
      "TODO: block destructive shell commands (rm -rf)",
      "TODO: block arbitrary network access (curl)"
    ]
  }
}
