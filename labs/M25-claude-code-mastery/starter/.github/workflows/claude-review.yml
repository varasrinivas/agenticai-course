# Claude PR Review Workflow
# TODO: Complete this GitHub Actions workflow

name: Claude PR Review

# TODO: Define the trigger
# Hint: on pull_request events (opened, synchronize)
# Hint: only for changes in src/ and tests/
on:
  pull_request:
    types: [opened, synchronize]
    # TODO: Add path filters

jobs:
  claude-review:
    runs-on: ubuntu-latest
    # TODO: Set permissions
    # Hint: contents: read, pull-requests: write
    permissions:
      contents: read
      # TODO: Add pull-requests permission

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # TODO: Add the review step
      # Requirements:
      #   - Use claude -p with a review prompt
      #   - Check for: UCC domain violations, missing error handling,
      #     security issues, missing test coverage
      #   - Use --output-format json
      #   - Use --session with a unique session ID per PR
      #     Hint: "pr-review-${{ github.event.pull_request.number }}"
      #   - Save output to review_result.json

      # TODO: Add the comment step
      # Requirements:
      #   - Run even if previous step fails (if: always())
      #   - Use gh pr comment to post results
      #   - Read from review_result.json
