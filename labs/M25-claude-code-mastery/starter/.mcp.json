{
  "$comment": [
    "Project-scoped MCP servers live HERE, not in .claude/settings.json.",
    "settings.json has no mcpServers key -- a server declared there is silently",
    "ignored, which is the same class of bug as a flattened hook.",
    "",
    "Read the DATABASE_URL from the environment rather than hardcoding it:",
    "this file is committed."
  ],
  "mcpServers": {
    "TODO: name the server": {
      "command": "TODO: npx",
      "args": ["TODO: -y", "TODO: the server package"],
      "env": {
        "TODO: VAR_NAME": "TODO: ${ENV_VAR}"
      }
    }
  }
}
