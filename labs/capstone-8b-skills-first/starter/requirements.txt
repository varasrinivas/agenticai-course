# Agent framework -- Tier 3: the SDK is the primary path, not a wrapper
# around client.messages.create().
claude-agent-sdk>=0.1.0

# Databases.
# python-oracledb thin mode: no Oracle Instant Client required.
oracledb>=2.4.0
psycopg[binary]>=3.2.0

# Tests.
pytest>=8.0.0
pytest-asyncio>=0.24.0
