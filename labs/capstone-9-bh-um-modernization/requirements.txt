# CAPSTONE-9 -- Behavioral Health UM Modernization
#
# Tier 3: the agent is built on claude-agent-sdk. There is no `anthropic`
# dependency here on purpose -- the only client.messages.create() in this lab
# is in appendix/manual-loop.py, which exists to show what the SDK is doing
# and installs its own dependency.

claude-agent-sdk>=0.2.0

# Tests. Everything under tests/ runs WITHOUT an API key: rules_ir,
# gap_register, seam_map, condition and validation carry no SDK import by
# design, so the divergence check and the eight parity checks run in CI.
pytest>=8.0

# Nothing else. The tools read files and parse SQL and XML with the standard
# library: a lab that needs a database driver to run its own tests is a lab
# most students will not run.
