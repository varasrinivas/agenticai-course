# Helm chart for the case service (Phase 6 / Track 6, M32). One parameterized chart per service;
# an umbrella chart (um-lite) would list these as dependencies to deploy the whole platform.
apiVersion: v2
name: um-case-svc
description: UM-Lite case service (Spring Boot)
type: application
version: 0.1.0
appVersion: "0.1.0"
