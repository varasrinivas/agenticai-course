# nginx config for the Angular SPA (Track 6, M30). Falls back to index.html so client-side routes work.
server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;   # SPA fallback
  }
}
