# Core — required for all labs
# Pinned below 1.0 deliberately. The 1.x SDK removed `temperature` from
# messages.create() -- it moved into output_config -- which breaks
# M01's temperature_lab outright, and that lab's whole subject is
# temperature. An unbounded >=0.39.0 installs 1.x today. Lift the
# ceiling only together with a migration of the labs that use it.
anthropic>=0.39.0,<1.0
python-dotenv>=1.0.0
pydantic>=2.0.0
tiktoken>=0.7.0

# RAG & Embeddings — M09, M10, M11, Capstones 2+
numpy>=1.26.0
chromadb>=0.5.0

# Data Pipelines — Capstone 5-6 (Domain C)
pyspark>=3.5.0

# Testing — M18, all capstones
pytest>=8.0.0
pytest-asyncio>=0.23.0

# API & Deployment — M21, M22, M22B
httpx>=0.27.0
fastapi>=0.115.0
uvicorn>=0.30.0

# Agent SDK — capstone-4 sdk_tests, capstone-8/8b, capstone-9, M26
# Imported by those labs but previously undeclared, so students installed
# whatever pip resolved that day.
claude-agent-sdk>=0.1.0

# Observability — M19, M20
structlog>=24.1.0
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
