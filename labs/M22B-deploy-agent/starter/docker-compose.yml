# M22B: Docker Compose for local development
# =============================================
# Complete the TODOs to orchestrate the UCC Agent container.
#
# Run:    docker compose up --build
# Stop:   docker compose down
# Logs:   docker compose logs -f

version: "3.8"

services:
  ucc-agent:
    # TODO 1: Build from the current directory (where the Dockerfile is)
    # build: .

    # TODO 2: Map port 8000 on your machine to port 8000 in the container
    # Format: "host_port:container_port"
    # ports:
    #   - "8000:8000"

    # TODO 3: Load environment variables from .env file
    # This keeps secrets (like API keys) out of docker-compose.yml
    # env_file:
    #   - .env

    # TODO 4: Set additional environment variables
    # PYTHONUNBUFFERED=1 ensures Python output appears in docker logs immediately
    # environment:
    #   - PYTHONUNBUFFERED=1
    #   - ENVIRONMENT=docker

    # TODO 5: Add a health check
    # Docker Compose can monitor container health and restart if needed.
    # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
    #   interval: 30s
    #   timeout: 5s
    #   retries: 3
    #   start_period: 10s

    # TODO 6: Set restart policy
    # "unless-stopped" restarts the container if it crashes,
    # but not if you explicitly stop it with `docker compose down`.
    # restart: unless-stopped

    # TODO 7: Volume mount for development (enables hot reload)
    # Mount current directory into /app so code changes are reflected
    # without rebuilding the image. Remove this in production.
    # volumes:
    #   - .:/app
