# Core — required for all labs
openai>=1.40.0
python-dotenv>=1.0.0
pydantic>=2.0.0
tiktoken>=0.7.0

# RAG & memory — M09, M10, M11, M22, CAPSTONE-C3
chromadb>=0.5.0
rank-bm25>=0.2.2

# API & deployment — M21, M21B, M22
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0

# M00B only — agent frameworks (comment out if installation is slow)
crewai>=0.80.0
# Pinned below 1.0 deliberately. LangChain 1.x replaced the agent API these
# labs teach -- create_openai_tools_agent and AgentExecutor are gone in favour
# of langchain.agents.create_agent -- so an unbounded >=0.3.0 installs 1.x and
# M00B's langchain_agent.py stops importing. Lift the ceiling only together
# with a rewrite of that lab.
langchain>=0.3.0,<1.0
langchain-community>=0.3.0,<0.4
langchain-ollama>=0.2.0   # ChatOllama lives here now, not in langchain-community
langchain-core>=0.3.0,<1.0

# M02 stretch goal only — exact Mistral tokenizer comparison (optional)
# transformers>=4.44.0
