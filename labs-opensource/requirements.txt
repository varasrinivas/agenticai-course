# Core — required for all labs
openai>=1.40.0
python-dotenv>=1.0.0
pydantic>=2.0.0
tiktoken>=0.7.0

# RAG & memory — M09, M10, M11, M22, CAPSTONE-C3
chromadb>=0.5.0
rank-bm25>=0.2.2

# API & deployment — M21, M21B, M22
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0

# M00B only — agent frameworks (comment out if installation is slow)
crewai>=0.80.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0

# M02 stretch goal only — exact Mistral tokenizer comparison (optional)
# transformers>=4.44.0
